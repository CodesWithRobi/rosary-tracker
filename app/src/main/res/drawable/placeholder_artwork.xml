<?xml version="1.0" encoding="utf-8"?>
<!-- Placeholder shown while mystery artwork loads, and on load failure.
     Warm card background with a thin gold border so the card never looks empty. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/background_card" />
    <stroke
        android:width="1dp"
        android:color="@color/gold_variant" />
</shape>